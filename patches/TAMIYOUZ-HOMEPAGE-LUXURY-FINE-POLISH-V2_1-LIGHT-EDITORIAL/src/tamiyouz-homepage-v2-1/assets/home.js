/* TAMIYOUZ-HOMEPAGE-LUXURY-FINE-POLISH-V2_1-LIGHT-EDITORIAL */
(function(){
  var root=document.documentElement,app=document.getElementById('tyz-home-v21');if(!app)return;
  var header=app.querySelector('[data-tyz-header]'),menu=app.querySelector('[data-tyz-menu]'),nav=app.querySelector('[data-tyz-nav]'),toggle=app.querySelector('[data-tyz-theme-toggle]'),meta=document.querySelector('meta[name="theme-color"]');
  function setTheme(theme,persist){theme=theme==='dark'?'dark':'light';root.setAttribute('data-tyz-theme',theme);if(meta)meta.setAttribute('content',theme==='dark'?'#0d0e10':'#fbfaf7');if(persist!==false){try{localStorage.setItem('tamiyouz-theme',theme)}catch(e){}}window.dispatchEvent(new CustomEvent('tamiyouz:v21theme',{detail:theme}))}
  setTheme(root.getAttribute('data-tyz-theme')==='dark'?'dark':'light',false);
  if(toggle)toggle.addEventListener('click',function(){setTheme(root.getAttribute('data-tyz-theme')==='dark'?'light':'dark',true)});
  function scrollHead(){if(header)header.classList.toggle('is-scrolled',window.scrollY>16)}scrollHead();window.addEventListener('scroll',scrollHead,{passive:true});
  if(menu&&nav){menu.addEventListener('click',function(){var open=!nav.classList.contains('is-open');nav.classList.toggle('is-open',open);menu.setAttribute('aria-expanded',open?'true':'false')});Array.prototype.forEach.call(nav.querySelectorAll('a'),function(a){a.addEventListener('click',function(){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false')})})}
  var reveals=Array.prototype.slice.call(app.querySelectorAll('.tyz-reveal'));
  if('IntersectionObserver'in window&&!window.matchMedia('(prefers-reduced-motion: reduce)').matches){var ob=new IntersectionObserver(function(entries){entries.forEach(function(entry){if(!entry.isIntersecting)return;entry.target.classList.add('is-visible');ob.unobserve(entry.target)})},{threshold:.1,rootMargin:'0px 0px -40px 0px'});reveals.forEach(function(el,i){el.style.transitionDelay=(Math.min(i%4,3)*55)+'ms';ob.observe(el)})}else{reveals.forEach(function(el){el.classList.add('is-visible')})}
  var canvas=document.getElementById('tyz-ai-canvas');if(!canvas)return;var ctx=canvas.getContext('2d');if(!ctx)return;var w=0,h=0,dpr=1,points=[],raf=null,reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  function build(){var count=Math.max(34,Math.min(70,Math.floor((w*h)/12000)));points=[];for(var i=0;i<count;i++)points.push({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.18,vy:(Math.random()-.5)*.18,r:Math.random()*1.3+.6})}
  function resize(){var r=canvas.getBoundingClientRect();w=Math.max(1,r.width);h=Math.max(1,r.height);dpr=Math.min(window.devicePixelRatio||1,2);canvas.width=Math.round(w*dpr);canvas.height=Math.round(h*dpr);ctx.setTransform(dpr,0,0,dpr,0,0);build()}
  function draw(){ctx.clearRect(0,0,w,h);var dark=root.getAttribute('data-tyz-theme')==='dark';var glow=ctx.createRadialGradient(w*.35,h*.42,0,w*.35,h*.42,Math.max(w,h)*.58);glow.addColorStop(0,dark?'rgba(212,168,78,.26)':'rgba(255,245,220,.74)');glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.fillRect(0,0,w,h);for(var i=0;i<points.length;i++){var p=points[i];if(!reduced){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1}ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=dark?'rgba(232,196,125,.62)':'rgba(171,119,34,.45)';ctx.fill();for(var j=i+1;j<points.length;j++){var q=points[j],dx=p.x-q.x,dy=p.y-q.y,dist=Math.sqrt(dx*dx+dy*dy);if(dist<110){ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.strokeStyle=dark?'rgba(222,180,98,.12)':'rgba(161,112,34,.11)';ctx.lineWidth=.7;ctx.stroke()}}}if(!reduced)raf=requestAnimationFrame(draw)}
  function redraw(){if(raf)cancelAnimationFrame(raf);resize();draw()}redraw();window.addEventListener('resize',redraw,{passive:true});window.addEventListener('tamiyouz:v21theme',function(){if(reduced)draw()});
})();
